meta {
  name: Record Prep
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/prep
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "itemId": "PREP_ITEM_UUID_HERE",
    "quantity": 5
  }
}
