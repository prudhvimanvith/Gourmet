meta {
  name: Adjust Stock
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/inventory/adjust
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "itemId": "ITEM_UUID_HERE",
    "change": 5,
    "reason": "Delivery"
  }
}
